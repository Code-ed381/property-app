import { LoginForm } from "./login-form";

export const metadata = {
  title: "Login | PILAS Properties",
  description: "Access the PILAS Properties management portal.",
};

export default function LoginPage() {
  return <LoginForm />;
}
